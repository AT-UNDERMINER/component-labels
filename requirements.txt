requests
pymupdf          # fitz — only used by the legacy component_lookup.py prototype now
                 # (the active pipeline no longer extracts anything from PDFs)
weasyprint
flask
python-dotenv
qrcode[pil]
Pillow
jinja2
openpyxl         # read/write .xlsx for the web UI batch import + template